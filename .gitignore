node_modules
dist
coverage
.DS_Store
*.log
.env

# Sensitive files
Dom�nios - Painel Registrobr.csv
configurar-dns.py
test-login.py
chaves.txt
marketing/chaves.txt


/petrobras
ssh-key-*.key

# Dados de usuarios / PII (senhas plaintext, emails) — NUNCA commitar
dados/usuarios.json
dados/newsletter.json

# Log de visitas contém IP + usuário de visitantes reais de produção (PII sob
# LGPD) e é reescrito a cada acesso ao site -- nunca deveria ter entrado no
# histórico do git (foi commitado por engano em f06ad8d). Removido do
# tracking; server.js continua lendo/escrevendo normalmente em disco na VM.
dados/visitas.json

# Bot de WhatsApp: pasta de sessão contém credenciais de autenticação da
# conta real conectada -- nunca versionar (reforço além do
# whatsapp-bot/.gitignore local).
whatsapp-bot/auth_info/
whatsapp-bot/node_modules/

# Cronogramas em HTML: gerados a partir de cronograma-16-semanas-completo.md
# pelos build-*.js. Nao versionados porque o workflow copia planos/ para dist/,
# o que os deixaria acessiveis sem login em /planos/*.html -- e o cronograma
# completo e conteudo do produto. Regerar com `node planos/build-impresso.js`
# e `node planos/build-artifact.js`.
petrobras-quimica-study-plan/planos/*.html
